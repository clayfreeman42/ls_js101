// START function to return the sum of two numbers

// SET first number

// SET second number

// SET output to first number + second number

// READ output and PRINT

// END

// START function that takes an array of strings and returns a string
// with those strings concatenated together

// SET array of strings

// SET output

// WHILE the iteration count is less than the number of strings in the array
// GET the string in the string of array that matches the iteration count and
// concatenate it to the output string

// RETURN output

// START function to merge two arrays of numbers with the first array populating the even index
// positions and the second array populating the odd positions

SET firstArray
SET secondArray

SET outputArray

SET counter = 0

WHILE counter <= firstArray length AND counter <= secondArray length

output array counter index 

SET counter to counter + 1

